月間みえる記録 PWA

公開方法:
1. このフォルダ内のファイルを、HTTPS対応のWebホスティングへアップロードします。
2. 公開URLをiPhoneのSafariで開きます。
3. 共有ボタン →「ホーム画面に追加」→「Web Appとして開く」をオンにして追加します。
4. 以後はホーム画面のアイコンから起動できます。

注意:
- 記録は端末内のブラウザ領域に保存されます。
- アプリ削除、SafariのWebサイトデータ削除、端末変更では記録が消える可能性があります。
